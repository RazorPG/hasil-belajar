/* file : luaslink.c*/
/* penulis : rafid hilmi, email rafidhilmi0504@gmail.com*/
/* membaca jari-jari, menghitung luas lingkaran*/
/* latihan pemakaian konstanta*/

#include <stdio.h>
int main()
{
    // JARAK
    // float v, t;

    // printf("selamat datang diprogram menghitung jarak\n")
    // printf("Kecepatan m/s : ");
    // scanf("%f", &v);
    // printf("waktu /s : ");
    // scanf("%f", &t);

    // printf("jaraknya adalah %.2f m", v * t);

    // LUASPERSEGI
    // float p;

    // printf("selamat datang diprogram menghitung luas persegi\n");
    // printf("masukkan nilai panjang persegi (meter) : ");
    // scanf("%f", &p);
    // printf("luas persegi adalah %.2f m2", p *= p);

    // KUBUS
    // float r;

    // printf("selamat datang diprogram menghitung volume kubus\n");
    // printf("masukkan rusuk kubus (cm) : ");
    // scanf("%f", &r);
    // printf("volume kubus adalah %.2f cm3", r * r * r);
}